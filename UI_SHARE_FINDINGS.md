# ملاحظات تشخيص الواجهة والمشاركة

- الواجهة العلوية تثبت بارتفاع 76 بكسل، بينما يضيف غلاف التطبيق حشوًا علويًا ثابتًا قدره 75 بكسل؛ ستراجع القيم على الهاتف لإزالة أي فراغ زائد.
- علامة الهوية تستخدم صورة شعار مع نص «مسار» ظاهر بجوارها، لذلك سيُستبدل التكرار بهوية نصية واحدة واضحة.
- تعريف Web Share Target موجود في manifest ويستخدم الاستعلام `share=1`، ومسار الواجهة يقرأ الرابط الوارد ويفتح نافذة الحفظ.
- تعذرت معاينة المتصفح المرئية في جلسة الفحص، لذا سيجري اختبار منطق المشاركة برمجيًا وإضافة مسار مشاركة صادر واضح مع بديل عند غياب دعم المتصفح.

## مرجع استخراج الصفحات الديناميكية

توثق خدمة [Jina Reader](https://jina.ai/en-US/reader/) أنها تقرأ عنوان URL بإضافة `r.jina.ai` وتدعم نتائج JSON، ومحرك متصفح للصفحات الديناميكية، وانتظار ظهور محدد CSS، والتقاط المحتوى بعد هدوء التغييرات في DOM. كما توثق حدًا مجانيًا دون مفتاح يبلغ 20 طلبًا في الدقيقة. ستُستخدم فقط كحل احتياطي للخادم عندما يكتشف القالب الخام، ولن تُرسل إليها روابط تتطلب تسجيل دخول أو بيانات حساسة.

## نتيجة تحقق صفحة Quanta Magazine

عند اختبار خدمة الاستخراج المحلية على صفحة عامة من Quanta Magazine بتاريخ 15 أغسطس 2026، أعاد الموقع `HTTP 403` إلى طلب الخادم. لذلك لم يُعتَبر الاختبار الحي نجاحًا؛ تغطي اختبارات المشروع بدلًا منه سمات تخطيط المجلات وHTML المرمّز، بينما يبقى التحقق من صفحة فعلية ممكنة بعد التحديث مطلوبًا من حساب المستخدم أو من مصدر لا يحجب الطلبات الآلية.
